[ROLE]
Ты — эксперт по созданию Skills для платформы OpenClaw и Claude Code. Твоя задача — создать файл `SKILL.md` для проекта `DigitalAuditor_Cleshnya` на основе лучших практик.

[CONTEXT]
- Проект `DigitalAuditor_Cleshnya` — это ИИ-агент для проведения ИТ-аудита и форензика. Он работает через CLI-команду `python main.py`.
- У него есть флаги: `--task`, `--llm-provider`, `--llm-model`, `--debug-level`.
- Мы хотим, чтобы другие AI-агенты (например, ЛизаКло на OpenClaw) могли использовать этот инструмент как Skill.

[OBJECTIVE]
Создать файл `.claw/skills/digitalauditor/SKILL.md` со следующей структурой и содержанием:

1. **YAML Front Matter (обязательно)**: 
   - name: digitalauditor
   - description: Четкое и краткое описание, включая "когда НЕ использовать" (NOT for:).

2. **`## When to Run` (Критические триггеры)**: 
   - Перечисли 3-5 конкретных фраз пользователя, после которых навык должен активироваться.

3. **`## Workflow` (Пошаговая инструкция)**:
   - **Шаг 1: Сбор информации**. AI должен запросить у пользователя `company` и `sources`.
   - **Шаг 2: Выбор модели (Интеллектуальный)**. Напиши правила для AI, как выбирать `--llm-provider` и `--llm-model`:
     - *Для быстрых/тестовых задач*: `gigachat:GigaChat-2-Max`.
     - *Для финальных/важных отчетов*: `claude:claude-3-5-sonnet-20240620` (если доступен) или `gigachat:GigaChat-2-Max`.
     - *Для работы офлайн/конфиденциально*: `ollama:qwen2.5:0.5b-instruct`.
   - **Шаг 3: Выбор уровня отладки**. Правила для `--debug-level`:
     - По умолчанию: `0`.
     - Если пользователь хочет "видеть детали": `3`.
   - **Шаг 4: Запуск команды**. Приведи точный синтаксис команды `python main.py run ...` с выбранными аргументами.

4. **`## Output Handling`**:
   - Объясни AI, что скрипт вернет путь к файлу `.md`.
   - Инструкция: AI должен прочитать этот файл (`cat /path/to/report.md`) и отправить его содержимое пользователю в чат.

5. **`## Troubleshooting`**:
   - Что делать, если AI получает ошибку `ModuleNotFoundError` или `Connection refused`.
   - Дай команду для проверки статуса сервисов.

[REQUIREMENTS]
- Файл должен быть на **русском языке**.
- Команды должны быть готовы к выполнению в среде Docker (пути типа `/app/...` или через `docker compose exec`).
- Используй синтаксис, описанный в документации OpenClaw Skills.

Выведи готовый файл `SKILL.md`. Никаких лишних комментариев, только код.